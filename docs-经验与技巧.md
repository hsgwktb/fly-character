# 果蝇数字角色 · Colab 部署经验与技巧

> 记录日期：2026-09-12
> 环境：Windows 10 · ZCode · Edge · Colab Pro（L4 GPU + high-RAM）· llama.cpp · cloudflared
> 配套件：`果蝇数字角色-Colab一键部署.ipynb`（21 格，从空运行时到可对话）
> 相关文档：`Colab运行Qwen3.8-27B-Uncensored-技能与报告.md`、`Colab-使用经验与技巧.md`、`果蝇数字角色-主动式架构设计与执行方案.md`

适用场景：在 Colab 上跑「真实连接组 + 大模型」的具身角色，并把观测台暴露到浏览器。
**不是** Colab 入门，也不是连接组科学本身。

---

## 0. 速查表（先看这个）

| 坑 | 现象 | 解法 |
|---|---|---|
| **`nohup` 挡不住 SIGINT** | 点"中断执行"解开卡死内核后，后台服务全死 | 用 **`setsid nohup ... < /dev/null &`** 另开进程组 |
| **`raw.githubusercontent` 有 CDN 缓存** | 推送后立刻拉，拿到**旧文件**（`?cb=` 无效）| 走 **GitHub API contents**，或 `git fetch + reset --hard` |
| **curl 覆盖过仓库文件后 `git pull` 静默失败** | "pull 成功但内容没变" | 用 `git reset --hard FETCH_HEAD` |
| **`/no_think` 关不掉思考** | 思考链照出，正文为空 | `chat_template_kwargs={"enable_thinking": false}` |
| **端口 8080 被占** | `couldn't bind HTTP server socket` | Colab 自带 node(pid 7) 占着，**用 8081** |
| **`<span>` 是 inline，宽高被忽略** | 条形图有数字没颜色，**控制台无任何报错** | CSS 加 `display:block` |
| **轮询/流式每帧重建 DOM** | 滚动位置每帧被重置 | `appendChild` 追加 + 粘性滚动 |
| **`push()` 里把形参 `s` 写成 `state`** | 图表在动，但**对话/思维/事件流全是空框**（看着像"变黑"）| `node tools/ui_smoke.js`（已接进构建，失败阻止推送）|
| **PyTorch CSR 行列 dtype 不一致** | CUDA `illegal memory access`，异步报错指错行 | CSR 的 `crow`/`col` 同为 int32 |
| **CSR 行内列未排序** | `device-side assert`（异步、误导性栈）| `np.lexsort((pre, post))` 规范化 |
| **长 cell 走 colab-mcp** | 调用挂起 → 会话被拖垮 | 后台进程 + 状态文件 + 短 cell 轮询 |
| **浏览器桥单连接槽位** | "连上又立刻断开" | **换端口**，旧标签页自然失效 |
| **`/api/reset` 把参数一起重置** | 消融开关被抹掉 | reset 时保留界面设的参数 |
| **状态包随历史增长** | 延迟从 1.6 s 爬到 6.7 s | 给 `recent_*` 设条数/长度上限 |

---

## 1. 一键笔记本跑了什么

| 步 | 内容 | 首次耗时 | 幂等 |
|---|---|---|---|
| 1 | 检查 GPU + 选档 | 秒 | — |
| 2 | 安装依赖（apt + pip） | 1–2 min | 是 |
| 3 | 取代码（GitHub clone / reset --hard） | 秒 | 是 |
| 4 | 取连接组数据（1.11 GB，免登录 GCS） | 1–5 min | 是（按文件跳过）|
| 5 | 建图（CSR + 神经递质符号） | ~5 min | 是（有产物即跳过）|
| 6 | 编译 llama.cpp（CUDA） | **10–20 min** | 是（有二进制即跳过）|
| 7 | 下载模型（14.4 GB，HF 断点续传） | 3–10 min | 是 |
| 8 | 起 llama-server + **闸门自测** | 1–2 min | 是 |
| 9 | 起角色服务 + 公网隧道 | ~1 min | 是 |
| 10 | 验收自测（4 项） | ~1 min | — |

**首次总计约 20–30 分钟**，其中 6+7 两步占 90% 的时间。之后重跑通常 2 分钟内起来。

**唯一非幂等的是切运行时**：`/content` 被清空，从第 4 步（数据）到第 7 步（模型）全要重来。
想长期用就把模型与图产物放 Drive（代价是 IO 慢）。

---

## 2. 硬件与显存预算（实测，不是估算）

实测配置：**L4，23,034 MiB 可用**，同时跑 27B 模型 + 连接组 LIF。

| 项 | 占用 |
|---|---|
| 27B IQ4_XS 权重 | 14.40 GB |
| KV cache（16K × 2 槽） | ~2.2 GB |
| 计算缓冲 + CUDA 上下文 | ~1.0 GB |
| 连接组 LIF（CSR 205 MB + 状态） | 实测约 3.0 GB |
| **合计** | **17,589 MiB / 23,034 MiB** |

结论：**L4 够用，余量约 5 GB**。留余量的意义是生成是**突发**的，不是均匀的。

### 参数取舍：为什么是 16K / 2 并发而不是 32K / 1

KV 是 `64 KiB/token`（该模型 64 层里只有 16 层 full attention，其余是 linear attention，
状态不随上下文增长）。所以：

| 配置 | KV | 总显存 | 评价 |
|---|---|---|---|
| 32K × 1 槽 | 2.15 GB | ≈ 同左 | 上下文长，但**你和它的对话会互相排队** |
| **16K × 2 槽** | 2.20 GB | ≈ 同左 | **推荐**：两个并发槽，16K 对角色足够 |

显存几乎一样，换来一个并发槽。角色的状态包只有几百 token，16K 绰绰有余。

---

## 3. 闸门：怎么关掉"思考"（接入的第一个卡点）

这个模型默认**先输出思考（`reasoning_content`）再输出正文（`content`）**，是两段不同字段。
对"说一句短话"这种任务，思考纯属浪费——实测 `max_tokens=2048` 时正文会**完全为空**。

三种关法，实测（同一提示，比较字数）：

| 方案 | 做法 | 思考字数 | 正文字数 | 结论 |
|---|---|---|---|---|
| A | 默认 | 93 | 5 | 会思考 |
| B | system prompt 里加 `/no_think` | 110 | 7 | **无效** |
| C | `chat_template_kwargs={"enable_thinking": false}` | **0** | 4 | **有效** |

笔记本第 8 格会**自动实测并落盘** `/content/llm_gate.json`，角色服务读它自动选对方案。

> **意外发现**：即使显式传 `enable_thinking: true`，对短提示也常常不产出思考链。
> 所以"思维"别指望 `reasoning_content`——真正可用的是**结构化输出里的 `thought` 字段**
> （由 system prompt 约束 JSON 格式）。思考链只当兜底。

### 延迟必须分段实测，不能拿聚合数字推

文档给出的 `6 tok/s` 是**吞吐**数字。我据此推算"每句要说 10–130 秒"，
于是判断必须大幅压低角色的发声率——**这个结论是错的**：

| 路径 | 实测往返 |
|---|---|
| 关思考 + 短输出（角色说话） | **0.7 – 1.6 s** |
| 开思考 + 详细分析 | 数十秒到分钟级 |
| 角色实时运行（状态包 + 对话历史） | **6.7 s**（见下）|

教训：**吞吐 ≠ 延迟**。关掉思考后，短输出场景完全够用对话。

---

## 4. 一个会随运行时间恶化的性能问题

角色实时运行中，认知层单次调用从 1.6 s 爬到 **6.7 s**。根因不是模型变慢，而是
**状态包随历史增长**：包里带了 `recent_thoughts / recent_says / recent_actions`，
每说一句话就多一条，提示词越来越长，而提示处理虽快（400 tok/s）也架不住累积。

修法（尚未实施）：给这些字段设**条数上限 + 每字段长度上限**，
或者把"近况"换成固定长度的摘要而不是原文。

> 通用教训：**给 LLM 的"状态"要有预算上限**。任何"把历史拼进 prompt"的设计
> 都会随时间劣化，必须在设计时就定上限。

---

## 5. 后台进程纪律（血泪集中区）

### 5.1 `nohup` 挡不住 SIGINT

我在 Colab 里点"中断执行"（Ctrl+M I）去解开一个卡死的 cell，结果把 `nohup` 起的
**llama-server 一起杀了**——`nohup` 只忽略 SIGHUP，而它仍留在内核的进程组里，
SIGINT 会传给整个前台进程组。现象是"中断内核之后模型服务也没了、显存掉回 3 GB"。

```python
# 错
subprocess.Popen("nohup python -u svc.py > log 2>&1 &", shell=True)
# 对：setsid 另开进程组，与内核解耦
subprocess.Popen("setsid nohup python -u svc.py > log 2>&1 < /dev/null &",
                 shell=True, executable="/bin/bash")
```

### 5.2 长任务不要塞进单个 cell

20 分钟的编译、5 分钟的建图，塞进一次调用里既看不到进度，也容易超时（见 §7）。
正解：**后台跑 + 状态文件**，然后短 cell 轮询。

```python
def status(m):
    open("/content/status.txt", "w").write(m)   # 另一条路看进度
    print(m, flush=True)                        # 进日志
```

### 5.3 幂等

后台任务必须能安全重入：`pkill -f xxx; sleep 1` 再起，产物则 `if os.path.exists(BIN): skip`。
否则重跑一次就有两个进程抢同一个端口。

### 5.4 GPU 饱和时 `nvidia-smi` 会变慢

12 核编译把 CPU 打满时，连带 `nvidia-smi`、`ps` 都可能变慢，让本来很快的 cell 看起来卡住。
**排查性能问题时要先确认自己不是在和编译抢资源。**

---

## 6. GitHub → Colab 的更新流程（两个静默陷阱）

推荐流程：

```bash
# 本地：构建并推送（带断言，防止把 base64 占位符推上去）
GH_TOKEN=... python build_repo.py --push "说明"
```

```python
# Colab：只更新前端（不需要重启服务，因为后端每次 GET / 都从磁盘读 ui.html）
import base64, json, urllib.request
req = urllib.request.Request(
    "https://api.github.com/repos/<user>/<repo>/contents/ui.html",
    headers={"Accept": "application/vnd.github+json", "User-Agent": "fly"})
d = json.loads(urllib.request.urlopen(req, timeout=30).read().decode())
open("/content/fly-code/ui.html", "wb").write(base64.b64decode(d["content"]))
```

### 陷阱 1：`raw.githubusercontent.com` 有 CDN 缓存

推送后**立刻**用 raw 拉会拿到**旧文件**，`?cb=<时间戳>` 也挡不住（参数被忽略）。
表现为"我明明推了，Colab 拉到的还是旧的"。我为此白折腾一轮。

解法：**走 GitHub API contents**（上面那段），或 `git fetch --depth 1 && git reset --hard FETCH_HEAD`。

### 陷阱 2：curl 覆盖过文件后 `git pull` 静默失败

曾经用 curl 直接覆盖过仓库里的文件 → 工作区有本地改动 → `git pull` **拒绝更新但不报错**
（"pull 成功但内容没变"）。比报错更难查。

解法：统一用 `git reset --hard FETCH_HEAD`，或者干脆走 API 拉单文件。

### 陷阱 3：手工 `cp` 会让"配置生成"退化

我把"后端从 `ui.html` 读页面"的版本，被后面几次 `cp cells/xxx repo/` 覆盖回了带
**base64 占位符**的版本 → 公网 `GET /` 变 **502**，而 **JSON 接口照常 200**，
我的所有自动检查都只看 JSON 字段，所以一路没发现。

解法：**用构建脚本统一产出 + 加断言**，不要手工 cp：

```python
assert "UI_B64" not in src, "webui_character.py 里还有 UI_B64 —— 页面会 502"
assert "__UI_B64__" not in out, "占位符没被替换"
ast.parse(out)
```

> **通用教训**：**"页面能不能打开"和"接口能不能返回"是两件独立的事，必须分别验证。**
> 只查 JSON 的验收会让你在页面已经 502 的情况下确信一切正常。

---

## 7. colab-mcp（浏览器桥）的脆弱性与绕法

它是**浏览器桥**，不是 API——每次操作都经过 Colab 前端这个重型 web 应用。
一天之内我撞到 **5 种不同故障**：

| # | 故障 | 现象 | 绕法 |
|---|---|---|---|
| 1 | 单连接槽位被抢 | 日志 `Server session was closed unexpectedly`，连上立刻断 | **换端口**（旧标签页自然连不上）|
| 2 | 前端进"无法保存更改"降级态 | `add_code_cell` 永久挂起，页面看着完全正常 | 重载页面 |
| 3 | `client.is_connected()` 说谎 | 底层 WS 已死它仍返回 True，REPL 死抱旧会话 | 以"来了更新的连接"为重建依据 |
| 4 | 挂起的调用无法打断 | 一次挂住的 `add_code_cell` 阻塞满超时，期间重连也醒不过来 | 让"工具调用"与"新连接"**赛跑**（`asyncio.wait(FIRST_COMPLETED)`）|
| 5 | 日志是追加的 | `grep REPL_READY` 匹配到**历史行**，误判"已就绪" | 先 `grep -n "WS server on" \| tail -1` 定位本次启动 |

### 另外三条

- **多端口监听**：`FLY_COLAB_PORTS=8899,8901` 同进程绑两个端口，浏览器里指向哪个的旧标签页都能连。
- **内核可能被卡死的 cell 永久占住**：此时 `add_code_cell` 正常、`run_code_cell` 全部挂起
  （这就是"一个 cell 永远跑不完，后面全排队"）。解法：命令面板 `Ctrl+Shift+P` -> 搜「中断执行」
  （快捷键 Ctrl+M I）。**中文命令用剪贴板粘贴**，别指望中文合成输入。
- **单条命令超时要两头都不合适**：太短（120 s）会杀掉正常的 cell；太长（300 s）会让一个挂起
  的调用堵住 5 分钟。经验值 **180 s**，并把长任务改成后台 + 状态文件（根本别让它长）。

> **最重要的策略结论**：**不要用浏览器桥做长时、改状态的工作。**
> 让它只做"投递一个短 cell"；一切长任务都在 Colab 侧后台跑，用状态文件/HTTP 接口看进度。
> 我最终给角色服务加了 `/api/diag` 与 `/api/probe` 两个端点，
> 于是**可以通过公网隧道观测和测试 Colab 侧**，基本不再依赖桥。这一步的收益最大。

---

## 8. 连接组侧的坑（与 Colab 无关，但会在这里遇到）

| 坑 | 现象 | 解法 |
|---|---|---|
| 64 位神经元 ID | 过一遍 float 或 JS 就精度损毁 | 全程 **int64**；`np.searchsorted` 映射，永不 round |
| 边表 1 GB | 一次性 `read_table().to_pandas()` 内存峰值高 | `pyarrow.ipc.open_file(pa.memory_map(...))` 按 batch 流式读 |
| **CSR 行列 dtype 混用** | CUDA `illegal memory access`，**异步报错指向无关的下一行** | `crow`/`col` 同为 int32 |
| **CSR 行内列未排序** | `device-side assert`（同样异步、栈误导）| `np.lexsort((pre, post))` 规范化后再建 |
| 不变量检查默认关闭 | 违反不变量的 CSR 会静默踩内存 | 建好后用 CPU + SciPy **对拍**，误差应为 0 |
| 神经递质列名 | 猜错列（`cell_type` vs `consensus_nt`）会得到无意义的符号分布 | 先打印列名再选，别用启发式 |

实测校验数（与官方公布一致）：**保留节点 166700 / 有向边 25582938 / 突触约 1.24 亿**，
且与 SciPy 稀疏矩阵对拍**最大绝对误差 0.000e+00**。

---

## 9. 前端三个"不报错"的坑

1. **`<span>` 默认 inline，`width`/`height` 被忽略** → 条形图有数字没有颜色，
   **浏览器控制台一个错都没有**。加 `display:block`。
2. **轮询/流式渲染不能重建 DOM**：每帧 `innerHTML = ...` 会把滚动位置重置。
   改为 `appendChild` 追加，并在追加前记录"是否本来就在底部"实现**粘性滚动**；
   容器加 `overscroll-behavior:contain` 防止滚到边缘时把滚动传给整页。
3. **`push()` 里写错变量名 → 后半段静默不执行**。实例：把形参 `s` 写成了 `state`
   （`state` 在该作用域不存在），抛 `ReferenceError`。它发生在**图表画完之后、事件渲染之前**，
   于是表现是：**图表在动、参数面板正常，但"对话/思维/事件流"全是空的深色框**——
   看起来像"窗口变黑了"，其实是那几块根本没被写入。

> 第 3 条特别值得记住：**异常的"位置"决定了你看到什么**。
> 同样的错误如果发生在 `push()` 开头，你看到的是"整页全死"，一眼就知道是 JS 问题；
> 发生在中段，你看到的是"大部分正常、某几块空着"，很容易误判成数据问题或 CSS 问题。

### 应对：无浏览器前端冒烟测试（`tools/ui_smoke.js`）

这类 bug 的共同点是**只在控制台报错、页面上表现为"某块是空的"**，靠肉眼很难定位。
仓库里带了一个用**假 DOM** 真跑一次 `push()` 的脚本，已接进构建流程：

```bash
node tools/ui_smoke.js          # 失败返回非 0
```

它做三件事：脚本能否加载（能抓住语法错误/顶层异常）、`push()` 是否抛异常、
以及各面板是否真的拿到了子节点（`chat`/`think`/`log`/三个条形图/认知面板）。
`build_repo.py` 在推送前会跑它，失败就**阻止推送**——这个机制第一次上线时就拦下了一次
（我自己把期望值写错了，它直接指出来）。

### 另外：给页面装了错误横幅

`ui.html` 顶部有一个全局 `error` / `unhandledrejection` 监听，任何未捕获异常会以红条
显示在页面顶部（带行号与累计次数）。目的是把"只在控制台可见的失败"变成"一眼可见"。
出现红条就说明**页面有脚本正在中断某部分更新**，截图反馈即可定位。

**验收必须包括"看一眼"。** 我这一天里三次被"测试全绿但界面是坏的"抓到：
一次是页面 502 而 JSON 照常 200、一次是条形图不着色、一次就是上面第 3 条的两个面板全空。

---

## 10. 安全边界（必须说清）

- **观测台页面不鉴权**：知道隧道地址 ≈ 有全部权限（可改参数、可重置、可发消息）。
  quick tunnel 地址是随机的，但**它就是密钥，不要外传**。
- **quick tunnel 是临时的**：地址重启即变，也可能自行掉线（HTTP 530），重跑第 9 格即可。
- **API Key**：`llama-server --api-key` + 网关的 401 逻辑能拦住乱扫与配错的客户端，
  但**同源页面本身不鉴权**，所以隧道地址仍是真正的秘密。
- **GitHub token**：用最小权限（只勾 `repo`）。我这次见到的 token 带
  `delete_repo` / `admin:org` / `admin:enterprise` —— 推代码的 token 不该能删仓库。
  **一旦明文出现在对话/日志里，立即撤销重建。**

---

## 11. 复现清单

```
□ 代码执行程序 -> 更改运行时类型 -> L4 GPU -> 保存
□ 全部运行（首次 20-30 分钟）
□ 第 8 格看"关思考方案"是哪一种（本环境实测是 C）
□ 第 9 格拿到：观测台网址 / Base URL / API Key / 模型名
□ 第 10 格四项验收全过
□ 打开观测台：图表在动、fps≈10、行为分布有更新
□ 在对话框发一句，等 30-60 秒看它自己是否开口（不保证回应，这是设计）
```

**验收标准**（本次实测均通过）：

1. `GET /` -> 200，页面正常渲染（条形有颜色、图表有曲线）
2. `/api/state` 字段齐备（`d/ro/interest/ci/u_speak/theta/appr/want/actions/affect/llm/...`）
3. `/api/probe`（关思考）-> 往返 < 3 s，`reasoning_chars = 0`
4. 发一条消息后，**它可能不回应**；若回应，台词应与其当前内驱力一致（可在思维面板核对）
5. 显存占用 < 80%（实测 17.6 / 23.0 GB）
6. `fps ≈ 10`（实时）

---

## 12. 两种对话模式（`reply` 开关）

界面上「认知层 → 强制回复我的输入」是一个二选一开关，对应两种**语义完全不同**的模式：

| `reply` | 模式 | 消息的角色 | 是否必回 |
|---|---|---|---|
| **0**（默认）| **主动模式** | 只是**刺激**：抬高社交显著性、兴趣、发声倾向 | **不保证**。说不说由它自己的冲动是否越过阈值决定 |
| **1** | **强制回复** | 明确要求回应的输入 | **必回**（绕过冲动阈值）|

主动模式是"由果蝇大脑主动驱动"这个设计的核心，**不要把它当坏掉的聊天机器人**：
它可能正在饿、正在躲、或单纯不想理你。强制模式则用于你想稳定对话时。

### 实现在哪里

- `hear()` 给每条消息加 `answered / taken / tries / taken_at` 四个状态字段
- 强制分支**必须放在自发发言之前判断**（见下面那个坑）
- 回复结果用 LLM 客户端的 `meta` 原样回传，把"这次生成"对回到"那条消息"
- 只有**成功且有台词**才算已回复；失败或空回复就交回队列重试（`tries` 上限 3）
- `taken` 超过 60 s 视为请求被队列丢弃（队列深度 1、latest-wins），也交回重试

### 坑：强制回复被自发发言"饿死"

第一版实现里，强制分支和自发发言**共用同一个 25 秒不应期**。结果：它一自发开口就把窗口占到 25 秒后，
在这期间到达的消息全被跳过——现象是"**消息明明进去了，但它从来不回**"，而且
`unanswered` 会一直累积。

修法两步：
1. **先判断有没有待回复的消息**；有则让自发发言让位（不再触发阈值路径）
2. 给回复路径一个**独立且更短的不应期**（8 秒）

> 通用教训：**两条都会"说话"的路径共享一个不应期时，先触发的那条会把另一条饿死。**
> 每个发声来源都该有自己的节奏控制。

### 坑：`/api/say` 在体解析失败时返回 `ok:true`

用 `curl -d '{"text":"中文"}'` 时字节会被破坏、服务端 JSON 解析失败 →
`text` 为空 → 消息根本没进系统，但接口**照样返回 `{"ok":true}`**。
这让"我的消息没被收到"看起来像"它不想理我"。

修法：空文本时返回 **400** 并给出提示（中文请用 `--data-binary @file.json`）。
**教训：接口不能对"没做成事"返回成功。**

### 一个容易误读的数字

界面/接口里的 `replies`（回复标记数）是从**事件流窗口**里数出来的，事件流只保留最近若干条，
所以旧回复滚出去后这个数会变小。它只适合看"最近有没有回复"，
**不适合当累计计数**——累计要看 `llm_consumed`。

---

## 13. 已知边界（别把这些当 bug）

1. **接地行为只有 4/11**：`eat / flee / approach / explore` 由识别细胞读出驱动，
   其余 7 个（drink/cool/warm/seek_humid/groom/court/rest）是回退到的手写权重。
   页面上每条行为都带 `连接组` / `回退` 标记，**回退比例必须一起报**。
2. **求偶与睡眠接不了地**：根因是建图时把多巴胺/血清素/章鱼胺的权重归零（8,432 个神经元），
   而它们正是觉醒门控的主力。实测注入这些群体**零传播**。
3. **消融只跑过单次**：`use=1` 时 explore 占 42%、`use=0` 时 0%，机制明确，
   但随机仿真需要多种子重复才能升级为统计结论。
4. **`reasoning_content` 不是主思维通道**（见 §3）。
5. **脑的时间尺度是压缩的**：默认每 100 ms 角色时间跑 15 ms 神经时间（0.15×），
   所以读出绝对值不可跨配置比较，只有相对排序可用。
